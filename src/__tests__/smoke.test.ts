// Smoke test — proves the jest-expo runner is wired.
// Replace/extend with real logic tests (hooks, lib functions) per feature.
describe("test infra", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
